/*
 * File:   classifier.h
 * Author: alexmc
 *
 * Created on November 19, 2014, 12:54 PM
 */

#include "classifier.h"

struct Classification *classify(volatile short fr[], volatile short fi[]) {
    struct Prob p1;
    struct Prob p2;
    struct Classification *guesses;
    // find p1 and p2
    guesses -> p1 = p1;
    guesses -> p2 = p2;
    return guesses;
}